# 検証結果

確認日：2026-09-24

## 自動チェック

`node --test tests/core.test.mjs`：6件成功、失敗0件。

## ブラウザ操作

ローカルサーバーをChrome / Playwrightで操作し、以下を確認しました。

- PC幅1365px、スマホ幅390pxで表示。画面の画像を目視確認。
- 初回のホーム表示、各ナビゲーション。
- STEP完了を保存 → 再読み込みで復元 → ホームの「続きから」で最初の未完了工程に移動。
- お気に入り登録 → 再読み込み → お気に入り一覧に表示。
- 写真アップロード → サンプル選択 → サイズ変更 → モック結果 → 写真つき制作ノート保存 → 再読み込み。
- 保存した作品のサイズ変更と復元。
- 辞典13項目の表示。
- 不正な動画URLの拒否、許可URLの保存と復元、元のURLに戻す。
- 不正な形式のアップロード時のエラー表示と提案ボタン無効化。
- スマホ幅で横方向のはみ出しがないこと。
- 操作中のブラウザJavaScript例外0件。

初回URLのルーティング不具合を検出し修正した後、上記の一連の操作を再実行して成功しました。

## 検証できていないもの

- 実機iPhone / Android、Safari固有の挙動。
- すべての動画の再生・利用可能な再生速度。参考先のページ存在を確認し、外部リンク方式を採用しています。
- 本番の画像AI解析（未実装・モック）。
- 編み図としての完成再現性や照明の取り付け安全性。
- WebMCP：オプションの2ツールをfeature-detectで登録しますが、今回のブラウザに`document.modelContext`がなく、ネイティブ登録・実行は検証できませんでした。通常の画面操作には影響しません。

## スマホ操作改善の追加検証

320 / 390 / 430 / 700 / 820 / 1280pxの6幅で、ホーム・2作品詳細・STEP・写真追加・辞典の計36画面を確認。横方向のはみ出しはありませんでした。

タッチ設定のChromeで、下部固定操作、完了して次へ、再読み込み、前のSTEP、取り消し、全工程完了、補助説明の開閉状態保持、写真相談の選択・サイズ保持を検証し成功。ブラウザJavaScript例外0件。390pxの作品・STEP画面を目視確認しました。実機Safariのキーボード・ホームインジケーター挙動は未検証です。
